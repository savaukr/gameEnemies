export interface IConfig {
    count: number;
    position: {
        x: number;
        y: number;
    };
}

export type TPosition = {
    x: number;
    y: number;
};

export type TSpeed = {
    x: number;
    y: number;
};
