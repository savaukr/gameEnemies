export const BtnConfig = {
    BOOSTER: "booster",
    PAUSE: "pause",
    MUTE: "unmute",
} as const;

export const CounterConfig = { TIMER: "timer", ENEMIES: "enemies" } as const;
export const maxTimerConfig = [10, 50, 40];
export const BOOSTER = 20;
